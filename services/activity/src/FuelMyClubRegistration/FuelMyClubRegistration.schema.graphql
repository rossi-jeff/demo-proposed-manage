type FuelMyClubRegistration {
  id: ID!
  registrationId: String
  data: String
  createdAt: String
  updatedAt: String

  Registration: Registration
}

extend type Query {
  fuelMyClubRegistrations: [FuelMyClubRegistration]
  fuelMyClubRegistration(id: ID!): FuelMyClubRegistration
}
