type Roster @key(fields: "id") {
  id: ID!
  groupId: String
  season: String
  final: Boolean
  createdAt: String
  updatedAt: String

  Group: Group
}

extend type Query {
  rosters: [Roster]
  roster(id: ID!): Roster
}
