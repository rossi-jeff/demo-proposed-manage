type SupportDocument {
  id: ID!
  name: String
  documentText: String
  sectionHeader: String
  createdAt: String
  updatedAt: String
}

extend type Query {
  supportDocuments: [SupportDocument]
  supportDocument(id: ID!): SupportDocument
}
