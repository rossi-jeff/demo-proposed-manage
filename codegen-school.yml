overwrite: true
schema: "services/school/**/*.schema.graphql"
documents: null
generates:
  services/school/generated/graphql.ts:
    config:
      federation: true
    plugins:
      - "typescript"
      - "typescript-resolvers"
